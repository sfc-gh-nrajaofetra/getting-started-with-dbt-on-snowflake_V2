## Project
name: marketing_leads_pilot
version: "1.0"
config-version: 2
profile: marketing_leads_pilot

## Sources
model-paths: [models]
analysis-paths: [analysis]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

## Target
target-path: target
clean-targets: [target, dbt_modules]

## Models
models:
  tasty_bytes:
    staging:
      +materialized: view
    marts:
      +materialized: table
